[project]
name = "rabbit-tun"
version = "0.1.0"
description = "Serial TCP tunnel"
requires-python = ">=3.10"
dependencies = [
    "pyserial",
]

[project.scripts]
rabbit-tun = "tunnel.cli:main"

[dependency-groups]
dev = [
    "nuitka",
    "pytest>=9.1.0",
]

