[project]
name = "rabbit-tun"
version = "0.1.0"
description = "Serial tunnel with SOCKS5 proxy support"
requires-python = ">=3.10"
dependencies = [
    "pyserial",
]

[dependency-groups]
dev = [
    "nuitka",
    "pytest>=9.1.0",
]

